---
phase: 01-one-command-start
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/start_mac.sh
  - scripts/stop_mac.sh
autonomous: true
requirements: [DEP-03, DEP-05, DEP-02, DB-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "scripts/start_mac.sh always runs docker build, so a stale image is never served; the --build flag is accepted and ignored for compatibility (D-04)"
    - "With no .env at the project root, scripts/start_mac.sh exits non-zero before building and prints a message naming .env.example and OPENROUTER_API_KEY (D-05)"
    - "After docker run, scripts/start_mac.sh polls http://localhost:8000/api/health and prints the URL only once the app answers; it never opens a browser (D-06)"
    - "Running scripts/start_mac.sh twice in a row is safe: the second run reports the app is already running and exits zero (DEP-03, DEP-01 concurrency edge)"
    - "scripts/stop_mac.sh stops and removes the finally-app container while preserving the finally-data volume, and is safe to run when nothing is running (DEP-03)"
    - "After stop_mac.sh then start_mac.sh, the database on the finally-data volume is the same database, proven by an unchanged users_profile.created_at rather than by values a re-seed would reproduce (DB-04, DEP-02, D-09)"
    - "The app receives its configuration from the project-root .env through docker run --env-file, and the file is never read or printed by the scripts themselves (DEP-05)"
    - statement: "If the container never becomes healthy, scripts/start_mac.sh prints the last container log lines, removes the container, and exits non-zero so a re-run starts clean (D-07). The happy path is proven by a real run; the failure branch is asserted from source only, because forcing a genuine never-healthy container would require building a deliberately broken image, which is out of scope for this phase."
      verification: backstop

  prohibitions:
    - requirement_id: DEP-03
      category: privacy
      statement: "MUST NOT print, echo, or persist the contents of .env or any API key value. The D-07 failure path may dump only the container's own application log output via docker logs --tail; it must never dump the container environment or inspect its config."
    - requirement_id: DEP-03
      category: safety
      statement: "MUST NOT stop, remove, or prune any Docker container, image, or volume the FinAlly scripts did not create. Every docker ps / rm filter stays anchored to ^finally-app$, and docker system prune is forbidden."

  artifacts:
    - path: "scripts/start_mac.sh"
      provides: "Always-build, env-guarded, health-polled container start with a clean failure path"
      contains: "api/health"
    - path: "scripts/stop_mac.sh"
      provides: "Container stop and removal that preserves the named data volume"
      contains: "finally-app"

  key_links:
    - from: "scripts/start_mac.sh"
      to: "backend/api/health.py"
      via: "curl poll against http://localhost:8000/api/health after docker run, before the URL is printed"
      pattern: "api/health"
    - from: "scripts/start_mac.sh"
      to: "docker-compose.yml"
      via: "shared image/container/volume names finally, finally-app, finally-data and port 8000, which must stay identical across both files"
      pattern: "finally-data"
    - from: "scripts/start_mac.sh"
      to: "the project-root .env"
      via: "docker run --env-file passes the file to the container without the script ever reading it"
      pattern: "env-file"
---

<objective>
Make `scripts/start_mac.sh` the one command that reliably starts FinAlly: always rebuild so the image is never stale (D-04), refuse to run without a `.env` (D-05), wait for the app to actually answer before printing the URL (D-06), and clean up after itself when the container never comes up (D-07). Then prove the whole start/stop/restart lifecycle with a real run (D-09).

Purpose: DEP-03 is the phase's headline promise — one command, and running it twice is safe. Today the script builds only when no image exists, merely warns about a missing `.env`, and prints the URL before the app can serve it.
Output: rewritten mac scripts plus run-based evidence for DEP-03, DEP-02 and DB-04.
</objective>

<execution_context>
@/Users/iselaalarcon/Documents/ia-curse/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/iselaalarcon/Documents/ia-curse/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-one-command-start/01-CONTEXT.md
@.planning/phases/01-one-command-start/01-PATTERNS.md
@.planning/phases/01-one-command-start/SKELETON.md
@.planning/phases/01-one-command-start/01-01-SUMMARY.md

Machine facts established at planning time (2026-09-20):
- **Port 8000 was occupied** by `tt_django_ctnr`, an unrelated container from another project that had been up two days, publishing `0.0.0.0:8000->8000/tcp`. It is NOT FinAlly. Do not stop it, do not remove it, do not ask it to move. Task 1 carries a precondition for this; if the port is still taken, halt and let the developer decide.
- `tt_pgadmin_ctnr` (5050) and `tt_postgres_ctnr` (5432) are likewise the developer's own containers.
- The `finally-data` volume did not exist yet, so the first real start creates and seeds it.
- The developer's `db/finally.db` (57 KB) is a separate local file used by the non-Docker test path (`test/scripts/start-app.sh` on port 8001). Docker never touches it. Leave it alone.
- `.env` exists at the project root and holds a live `OPENROUTER_API_KEY`. Never read, print, or commit it.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rewrite start_mac.sh and prove one complete start end-to-end</name>
  <precondition>TCP port 8000 is free: `docker ps --format '{{.Ports}}'` shows no container publishing 8000, and `lsof -ti:8000` returns nothing. At planning time the unrelated container `tt_django_ctnr` held this port; it belongs to another project and must NOT be stopped without the developer's explicit say-so.</precondition>
  <reversibility rating="reversible">All four changes are local edits to one shell script behind a stable CLI; reverting is a single git checkout with no dependent contract.</reversibility>
  <files>scripts/start_mac.sh</files>
  <read_first>
    - scripts/start_mac.sh (all 47 lines; this is the file being modified and its own best analog)
    - .planning/phases/01-one-command-start/01-PATTERNS.md (section "scripts/start_mac.sh"; it marks each line range to keep verbatim, to delete, and to change, and gives the repo's stderr conventions)
    - .planning/phases/01-one-command-start/01-CONTEXT.md (decisions D-04 through D-07 verbatim)
    - docker-compose.yml (the names and port that must stay identical: image finally, container finally-app, volume finally-data, 8000)
    - Dockerfile (the HEALTHCHECK added by plan 01-01, which the poll complements)
  </read_first>
  <action>
    Rewrite `scripts/start_mac.sh`, keeping its existing constants block (`IMAGE_NAME=finally`, `CONTAINER_NAME=finally-app`, `VOLUME_NAME=finally-data`, `PORT=8000`), its `SCRIPT_DIR`/`PROJECT_ROOT` resolution, and its `docker run -d` flags exactly as they are. Four behavior changes:

    **D-05 first, before anything else.** Move the project-root `.env` existence check to the top of the script, above the already-running check and above the build. Turn the current warning into a hard failure: write to stderr and `exit 1`. Keep the existing wording, which already matches the decision: it must name `.env.example` and `OPENROUTER_API_KEY`. Placing it first is a deliberate discretion call — it makes the failure deterministic regardless of container state, and it is what lets the check be tested without disturbing the developer's real `.env`.

    **D-04: always build.** Delete the `BUILD` variable and the `--build` argument loop, and drop the image-existence condition currently guarding the build (lines 31-34, marked in the pattern map) so `docker build -t finally .` runs unconditionally. Docker's layer cache makes an unchanged rebuild near-instant. Keep accepting a `--build` argument silently so old invocations do not error; simply ignore it. Update the header comment so it no longer says the build is conditional.

    **D-06: poll, then print.** After `docker run -d`, poll `http://localhost:8000/api/health` until it answers, then print the running URL. Use `curl -fs` with output discarded. Choose 60 attempts at 1-second intervals (Claude's discretion; the observed cold start is about 5 seconds, and the image HEALTHCHECK uses a 10-second start period). `set -e` is active, so guard the probe inside an `if` or with `||` so a failed attempt does not abort the script. Do not open a browser.

    **D-07: clean failure.** If the poll never succeeds, print the last 30 container log lines to stderr with `docker logs --tail 30`, then `docker rm -f` the container and `exit 1`, so the next run starts clean. Dump only the container's own application log output — never the container environment or its config, which would echo the API key into the terminal.

    Keep every `docker ps`/`docker rm` filter anchored as `^finally-app$` so no unrelated container can ever match.

    Then prove the happy path with one real run: the script builds, starts, waits, and prints the URL, and the app answers on port 8000.
  </action>
  <verify>
    <automated>
cd /Users/iselaalarcon/Documents/ia-curse/finally
set -e
bash -n scripts/start_mac.sh
grep -q 'docker logs --tail' scripts/start_mac.sh
grep -q 'api/health' scripts/start_mac.sh
test "$(grep -v '^#' scripts/start_mac.sh | grep -c 'docker images -q')" = "0"
test "$(grep -v '^#' scripts/start_mac.sh | grep -c 'printenv')" = "0"
test "$(grep -v '^#' scripts/start_mac.sh | grep -c 'open http')" = "0"
docker rm -f finally-app >/dev/null 2>&1 || true
./scripts/start_mac.sh | tee /tmp/finally-start.out
grep -q 'http://localhost:8000' /tmp/finally-start.out
curl -fs http://localhost:8000/api/health | grep -q '"status":"ok"'
test "$(curl -fs http://localhost:8000/api/watchlist | grep -o '"ticker"' | wc -l | tr -d ' ')" = "10"
echo "START PASS"
    </automated>
    <fails_when>non-zero exit at any step, or the final line is not "START PASS". If the script prints the URL before the app is ready, the curl assertion fails with a connection refused; a leftover `docker images -q` gate makes the count assertion non-zero.</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/start_mac.sh` exits 0.
    - `scripts/start_mac.sh` contains no `docker images -q` gate and no `BUILD` variable, counted with comment lines filtered out (D-04).
    - The `.env` check appears earlier in the file than both the already-running check and the `docker build` line (D-05); confirm with the line numbers from `grep -n`.
    - The `.env` failure message contains the strings `.env.example` and `OPENROUTER_API_KEY` and is written to stderr followed by `exit 1`.
    - The script contains `docker logs --tail 30` and a `docker rm -f` on the failure branch, followed by `exit 1` (D-07).
    - The script contains no command that dumps the container environment, and no browser-opening command (D-06).
    - Every container-name filter in the file uses the anchored form `^finally-app$`.
    - A real run prints a line containing `http://localhost:8000`, and immediately afterwards `/api/health` returns `"status":"ok"` and the watchlist returns 10 tickers.
  </acceptance_criteria>
  <done>start_mac.sh implements D-04 through D-07 and one real run proves the app is genuinely serving before the URL is printed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the full lifecycle — idempotence, stop, restart, persistence</name>
  <precondition>The finally-app container is running and answering on port 8000 from Task 1.</precondition>
  <files>scripts/stop_mac.sh</files>
  <read_first>
    - scripts/stop_mac.sh (all 15 lines; the file being checked, expected to need no change)
    - scripts/start_mac.sh (as rewritten in Task 1)
    - .planning/phases/01-one-command-start/01-PATTERNS.md (section "scripts/stop_mac.sh", which records that the current file is already correct and preserves the volume)
    - .planning/phases/01-one-command-start/01-CONTEXT.md (D-09, which defines exactly this evidence)
  </read_first>
  <action>
    Confirm `scripts/stop_mac.sh` already satisfies DEP-03: it exits zero with a message when nothing is running, stops and removes only the anchored `finally-app` container, and never touches the `finally-data` volume. Change it only if one of those is false; the expectation from the pattern map is no change.

    Then run the D-09 evidence sequence end-to-end against the real scripts and the real `finally-data` volume, in this order: run the start script a second time and confirm it reports the app is already running and exits zero; read `users_profile.created_at` out of the running container; run the stop script and confirm the container is gone while the `finally-data` volume is still listed; run the start script again; read `created_at` a second time and require it to be identical to the first.

    Use the seed timestamp, not cash or ticker counts, as the persistence signal. A destroyed volume would be recreated and re-seeded to the same 10000.0 and the same ten symbols, so those values prove nothing; the seed timestamp changes on every fresh seed. This was confirmed fail-first at planning time.

    Finish by running the stop script one last time so port 8000 is released and the machine is left clean, with the `finally-data` volume preserved. Never remove that volume.
  </action>
  <verify>
    <automated>
cd /Users/iselaalarcon/Documents/ia-curse/finally
set -e
bash -n scripts/stop_mac.sh
test "$(grep -v '^#' scripts/stop_mac.sh | grep -c 'volume')" = "0"
./scripts/start_mac.sh | tee /tmp/finally-second.out
grep -qi 'already running' /tmp/finally-second.out
BEFORE=$(docker exec finally-app python -c "import sqlite3;print(sqlite3.connect('/app/db/finally.db').execute('select created_at from users_profile').fetchone()[0])")
test -n "$BEFORE"
./scripts/stop_mac.sh
test -z "$(docker ps -aq -f name='^finally-app$')"
docker volume ls --format '{{.Name}}' | grep -qx 'finally-data'
./scripts/start_mac.sh >/dev/null
for i in $(seq 1 60); do curl -fs http://localhost:8000/api/health >/dev/null 2>&1 && break || sleep 1; done
AFTER=$(docker exec finally-app python -c "import sqlite3;print(sqlite3.connect('/app/db/finally.db').execute('select created_at from users_profile').fetchone()[0])")
test "$BEFORE" = "$AFTER"
./scripts/stop_mac.sh
docker volume ls --format '{{.Name}}' | grep -qx 'finally-data'
echo "LIFECYCLE PASS"
    </automated>
    <fails_when>non-zero exit at any step, or the final line is not "LIFECYCLE PASS". A second start that rebuilds and re-runs instead of reporting "already running" fails the grep; a stop that removed the volume fails the `grep -qx finally-data` check; a seed timestamp that changed across the stop/start cycle fails the persistence assertion, which is exactly what a destroyed volume produces.</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/stop_mac.sh` exits 0 and the file references no volume at all (it must not remove one).
    - A second `./scripts/start_mac.sh` while running prints a line matching `already running` and exits 0 (DEP-03 idempotence).
    - After `./scripts/stop_mac.sh`, `docker ps -aq -f name='^finally-app$'` prints nothing.
    - After that same stop, `docker volume ls` still lists `finally-data` exactly (DEP-02).
    - `users_profile.created_at` is byte-identical before the stop and after the next start (DB-04).
    - `./scripts/stop_mac.sh` run when nothing is running exits 0 and prints that FinAlly is not running.
    - At task end, port 8000 is free and `finally-data` still exists.
  </acceptance_criteria>
  <done>D-09's evidence sequence is complete: start twice is safe, stop preserves the volume, and the restarted app is reading the same database.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the missing-env refusal without touching the real .env</name>
  <files>(verification only; no file is modified)</files>
  <read_first>
    - scripts/start_mac.sh (as rewritten in Task 1; confirm the env check really is the first thing the script does)
    - .planning/phases/01-one-command-start/01-CONTEXT.md (D-05 and D-07)
  </read_first>
  <action>
    Prove D-05 by running the real script from a throwaway copy, never by moving or deleting the developer's `.env`. Create a temporary directory, make a `scripts/` subdirectory inside it, copy `scripts/start_mac.sh` there, and run the copy. The script resolves its project root from its own location, so the copy sees a root with no env file and must exit non-zero with the expected message before it reaches any `docker build`. Delete the temporary directory afterwards.

    This is exactly why the env check is ordered first in Task 1: it fires regardless of what containers are running, so the test is deterministic and needs no access to the real secret.

    Then cover the D-07 failure branch by source assertion, since forcing a genuinely never-healthy container would mean building a deliberately broken image. Assert the branch dumps bounded log output, force-removes the anchored container, and exits non-zero. Record in the summary that the D-07 failure path is verified from source rather than by execution, matching the backstop truth in this plan's must_haves.
  </action>
  <verify>
    <automated>
cd /Users/iselaalarcon/Documents/ia-curse/finally
set -e
T=$(mktemp -d)
mkdir -p "$T/scripts"
cp scripts/start_mac.sh "$T/scripts/start_mac.sh"
set +e
OUT=$("$T/scripts/start_mac.sh" 2>&1); RC=$?
set -e
rm -rf "$T"
test "$RC" -ne 0
printf '%s' "$OUT" | grep -q '.env.example'
printf '%s' "$OUT" | grep -q 'OPENROUTER_API_KEY'
if printf '%s' "$OUT" | grep -qi 'Building'; then echo "FAIL: built despite a missing env file"; exit 1; fi
test -f .env
echo "ENVGUARD PASS"
    </automated>
    <fails_when>non-zero exit, or the final line is not "ENVGUARD PASS". An exit code of 0 from the copied script means the guard is missing or is ordered after the already-running check; output lacking the two expected strings means the message was reworded away from D-05; a missing project-root .env at the end means the test disturbed the developer's file.</fails_when>
    <automated>cd /Users/iselaalarcon/Documents/ia-curse/finally && grep -A4 'docker logs --tail' scripts/start_mac.sh | grep -q 'docker rm -f' && grep -A6 'docker logs --tail' scripts/start_mac.sh | grep -q 'exit 1' && echo FAILPATH_OK</automated>
    <fails_when>non-zero exit, or "FAILPATH_OK" is absent — the failure branch is missing its cleanup or its non-zero exit, so a failed start would leave a dead container behind and report success</fails_when>
  </verify>
  <acceptance_criteria>
    - A copy of `scripts/start_mac.sh` run from a directory with no `.env` exits non-zero.
    - That run's combined output contains both `.env.example` and `OPENROUTER_API_KEY` (D-05).
    - That run produces no build output, proving the guard fires before `docker build`.
    - The project-root `.env` still exists after the task, and its contents were never read, printed, or copied.
    - The temporary directory is removed.
    - Within 6 lines of `docker logs --tail` in the script there is both a `docker rm -f` and an `exit 1` (D-07).
    - The SUMMARY records D-07's failure branch as source-verified, not execution-verified.
  </acceptance_criteria>
  <done>The missing-env refusal is proven by running the real script safely, and the failure branch is asserted from source with the limitation recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project-root `.env` → container | `--env-file` hands a live API key to the container at run time |
| container stderr/stdout → developer terminal | the D-07 failure path prints container output on screen |
| script → Docker daemon | the script creates and destroys containers by name |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | D-07 failure path printing container output | high | mitigate | dump only bounded application log output with `docker logs --tail 30`; Task 1 asserts the script contains no command that dumps the container environment or config, so the API key cannot reach the terminal |
| T-01-07 | Tampering | `docker rm -f` on the failure path and `docker rm` in the idempotence path | high | mitigate | every filter is anchored `^finally-app$`, so no unrelated container can match; Task 1 asserts the anchored form is used everywhere and the prohibitions block forbids touching anything the scripts did not create |
| T-01-08 | Information Disclosure | testing the D-05 guard | medium | mitigate | the guard is exercised from a throwaway copy in `mktemp -d`; the real `.env` is never moved, renamed, read, or deleted, and Task 3 asserts it still exists afterwards |
| T-01-09 | Denial of Service | publishing port 8000 while another project's container wants it | medium | accept | surfaced as a Task 1 precondition so a human decides; the plan never reassigns or stops another project's container |
| T-01-10 | Information Disclosure | `--env-file` passes every variable in `.env` into the container | low | accept | the file holds only this app's three documented variables (`OPENROUTER_API_KEY`, `MASSIVE_API_KEY`, `LLM_MOCK`) per `.env.example`; scoping per-variable is unnecessary complexity at ASVS L1 |
</threat_model>

<verification>
- `START PASS`, `LIFECYCLE PASS`, `ENVGUARD PASS` and `FAILPATH_OK` all print with zero exits.
- `finally-data` still exists and `db/finally.db` is unchanged.
- `tt_django_ctnr`, `tt_pgadmin_ctnr` and `tt_postgres_ctnr` are untouched.
- Port 8000 is released at the end.

**Flagged assumption carried out of this plan:** the D-07 failure path is verified from source, not by execution, and is recorded as a `backstop` truth. Producing a genuinely never-healthy container would require building a deliberately broken image, which this phase does not do. The happy path, the missing-env refusal and the idempotence path are all execution-verified.
</verification>

<success_criteria>
DEP-03 is proven by real runs of both mac scripts, DEP-02 and DB-04 are proven through the real `finally-data` volume, and D-04, D-05, D-06, D-07 and D-09 are each satisfied with command output.
</success_criteria>

## Artifacts this phase produces

Changed by this plan:

| Artifact | Kind | Detail |
|---|---|---|
| `scripts/start_mac.sh` | shell script | env guard moved to the top and made fatal; `BUILD` var and `--build` loop removed; unconditional `docker build`; health poll loop against `http://localhost:8000/api/health`; failure branch with `docker logs --tail 30` + `docker rm -f` + `exit 1` |
| `scripts/stop_mac.sh` | shell script | verified unchanged: stops and removes `finally-app`, preserves `finally-data` |

Symbols and identifiers this plan fixes for later phases:

Shell constants `IMAGE_NAME=finally`, `CONTAINER_NAME=finally-app`, `VOLUME_NAME=finally-data`, `PORT=8000` · health URL `http://localhost:8000/api/health` · poll budget 60 attempts at 1s · log tail 30 lines · anchored name filter `^finally-app$` · `--build` accepted and ignored · exit codes: 0 on success and on already-running, 1 on missing env and on never-healthy.

<output>
Create `.planning/phases/01-one-command-start/01-02-SUMMARY.md` when done.
</output>
