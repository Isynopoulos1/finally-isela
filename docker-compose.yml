services:
  app:
    build: .
    image: finally
    container_name: finally-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - finally-data:/app/db

volumes:
  finally-data:
